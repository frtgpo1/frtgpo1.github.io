DOM SELECTORS
Standard
getElementsByTagName("a");
getElementsByClassName("wrapper");
getElementById("logo");

Query Selector - Kann alle CSS Selektoren ansteuern
// gibt das erste gefundene Element zurück
querySelector("h1");
// gibt alle zutreffenden Elemente zurück
querySelectorAll("h4", "li").
Beispiele:
querySelectorAll("[for=first-name]");
querySelector("body>form>input[type=submit]");

Attributes
element.getAttribute("for");
element.setAttribte("for", "submit");

Style Property			/* Seperation Of Concerns Bruch */
element.style.font-size = 23;
element.style.color = "green';

Class Name / Class List	/* Besser SOC */
/* Böse, ganz BÖSE! Alle bisherigen Klassen werden ersetzt */
element.className = "header";
/* besser */
element.classList.add
				 .remove
				 .toggle

Inner HTML
/* innerHTML ist gefährlich! */
/* Script irgendwas Fehlerquelle (letztes Video bei DOM sagt er den Namen) */
/* Bei innerHTML wird viel nachgeladen (Paint Flashing) */
element.innerHTML = "<strong>!!!!</strong>"


Parent Element / Child Element
element.parentElement;
element.children;



Nicht in Kurs / Ergänzt:
/* führt Funktion nach einer bestimmten Zeit aus */
window.setTimeout(funcName, 555<millisekunden>, funcArg1, funcArg2);
/* führt Funktion in bestimmten Zeitabständen aus */
window.setInterval(funcName, 555<millisekunden>, funcArg1, funcArg2);



ELEMENTE ERSTELLEN UND EINFÜGEN
const li = document.createElement("li");
const node = document.createTextNode("value")
li.appendChild(node);

const firstForm = document.querySelector(".first-form");
firstForm.appendChild(li);
ODER
const firstFormChild = document.querySelector(".first-form>label");
firstForm.insertBefore(li, firstFormChild);



WERTE AUS HTML ELEMENTEN LESEN
Wert des Input Feldes
const inputField = document.querySelector("input#input-one");
console.log(inputField.value);


EVENTS
https://developer.mozilla.org/en-US/docs/Web/Events
90% der Zeit -> Maus und Keyboard-Events

button.addEventListener("click, myFunction");
button.addEventListener("click, () => {}");

Wert der gedrückten Taste
inputOne.addEventListener("keypress", (e) => {
    if (event.which == 13)
    addInputValueToList();
} );
